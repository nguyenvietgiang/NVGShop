<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nvgshop">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NVGShop"
        tools:replace="android:label">
        <activity
            android:name=".admin.AccountManagerActivity"
            android:exported="true" />
        <activity
            android:name=".admin.ProductManagerActivity"
            android:exported="true" />
        <activity
            android:name=".admin.DashbroadActivity"
            android:exported="true" />
        <activity
            android:name=".portal.AccountActivity"
            android:exported="true" />
        <activity
            android:name=".portal.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".portal.MainActivity"
            android:exported="true" />
        <activity
            android:name=".portal.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>